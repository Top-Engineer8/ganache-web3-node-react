import database file into phpmyadmin
change on nodejs-server/config/db.js as your configuration settings of phpmyadmin
run the project by (npm run start)


reactjs-client:
Smart contract fild: cities.sol inside src/contracts
Backend code file: cities.js inside src/data
compile the contract to Ganache by (truffle migrate --reset)
run the project by (npm run start)

